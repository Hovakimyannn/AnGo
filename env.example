APP_ENV=prod # TODO change to prod for css build
APP_SECRET=CHANGE_ME_TO_A_RANDOM_32+_CHAR_STRING
APP_DEBUG=1

# Public base URL (used for generating correct absolute links in emails)
# APP_URL="https://an-gor.am"

# Local/dev (non-docker) default:
DATABASE_URL="postgresql://postgres:postgres_password@127.0.0.1:5432/salon_db?serverVersion=16&charset=utf8"

# Optional (if you run Redis locally)
REDIS_URL="redis://127.0.0.1:6379"

# Mailer
# SendGrid (recommended): API transport (avoids SMTP port blocks/timeouts)
# MAILER_DSN="sendgrid+api://YOUR_SENDGRID_API_KEY@default"
#
# SendGrid SMTP (fallback):
# MAILER_DSN="smtp://apikey:YOUR_SENDGRID_API_KEY@smtp.sendgrid.net:587"
# (Alternative SMTP port that is often unblocked: 2525)
# MAILER_DSN="smtp://apikey:YOUR_SENDGRID_API_KEY@smtp.sendgrid.net:2525"
MAILER_DSN="null://null"
MAILER_FROM="AnGo <noreply@example.com>"
