# This project is deployed using the Dockerfile (see railway.json).
# Keep this file minimal to avoid overriding the Dockerfile CMD.

[deploy]
# Use the image entrypoint (nginx + php-fpm).
startCommand = "/entrypoint.sh"
